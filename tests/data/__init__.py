"""Level 2 — data tests: domain correctness.

Salary table values, seniority increments, and apprenticeship percentages are
loaded via ``load_ccnl()`` from the real knowledge bundle and compared against
pre-recorded case files. No ``compute()``, no tax logic — data layer only.
"""
