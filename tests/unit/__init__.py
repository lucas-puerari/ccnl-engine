"""Level 1 — unit tests: code correctness.

Algorithms, Pydantic validators, and serialisation logic are exercised in
isolation using synthetic CCNL objects built from factory helpers. No real
knowledge JSON files are loaded; no tax computation is invoked.
"""
