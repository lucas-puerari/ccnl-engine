"""CCNL contract bounded context: domain models and data loader.

The engine never stores CCNL data;
:func:`~ccnl_engine.contract.service.loaders.load_ccnl` reads the
versioned datasets from :mod:`ccnl_engine.knowledge`.
"""
