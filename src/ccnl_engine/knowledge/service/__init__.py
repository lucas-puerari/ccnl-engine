"""Knowledge service layer: bundled resource readers and the bundled repository.

The only Python logic in :mod:`ccnl_engine.knowledge`. It reads the passive
JSON resources under ``knowledge/*/data/`` and never writes them.
"""
