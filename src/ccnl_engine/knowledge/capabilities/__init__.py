"""Fiscal capability catalog datasets: versioned per-year feature declarations.

One file per year under ``ccnl_engine/knowledge/capabilities/data/``.
Each file lists every fiscal feature the engine handles for that year,
together with its implementation status.
"""
