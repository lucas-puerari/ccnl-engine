"""Validation harness namespace (placeholder).

Validation logic is currently shipped as golden tests (``tests/golden``).
This package exists so the engine / knowledge split has a future home for
runnable cross-checks (e.g. back-calculated rates, data integrity scripts)
without polluting :mod:`ccnl_engine.engine` or :mod:`ccnl_engine.knowledge`.
"""
